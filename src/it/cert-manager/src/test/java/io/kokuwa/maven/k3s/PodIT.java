package io.kokuwa.maven.k3s;

import org.junit.jupiter.api.Test;

public class PodIT {

	@Test
	void test() {
		// the test should only trigger the deployment and assert it does not fail because of the completed job.
	}
}
